"""OpenVLC desktop control panel.

A PySide6 desktop app that orchestrates the OpenVLC video link from the PC over
SSH, so the Raspberry Pi receiver stays dedicated to its latency-sensitive
bridge. See README.md.
"""

__version__ = "0.1.0"
